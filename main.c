#include <stdio.h>
#include <stdlib.h>
#include "monster.h"
#include "menu.h"

#define SPEED 1500

int main(int argc, char** argv){

    // play();
    menu();
    // MAP* map = getMap(4);
    // MONSTER_LIST* monster_list = spawn(5,5,5,map);
    // printMonsters(monster_list);

    return 0;
}